# TMDB Movie Data Analysis using Pandas, Python and APIs

This project is aimed at building a data analysis pipeline using Python and Pandas by extracting movie Data from the TMDB then cleaning and performing analysis on the extracted data


## Requirements for staring the project
- A TMDB account for access to Api key and Access Token for authorisation for the usage of the API
- Have python installed
- A virtual environment for package installations
- Have revelant packages installed

## Installation and Setup
- First create a virtual environent to have all your packages installed there `python -m venv venv`
- Run `pip install -r requirements.txt` to install the required packages needed for the project
- Create a `.env` file and have this in there
  ```python
    TMDB_API_KEY=API_KEY

## Starting the application
- Run the noteook in your preferred editor to get all the outputs
